import React from 'react';

export default class Settings extends React.Component {
  render() {
    return (
      <div>
        <h5> The possibility to adjust settings will be added later </h5>
      </div>
    );
  }
}

Settings.propTypes = {
};
